from pathlib import Path

# relative paths to symbol SVGs stored as a constant

BASE_PATH = Path(__file__).parent / "glyphs"

OK = str(BASE_PATH / "ok.svg")

MOVING = str(BASE_PATH / "moving.svg")

WARNING = str(BASE_PATH / "warning.svg")

ERROR = str(BASE_PATH / "error.svg")

DISCONNECTED = str(BASE_PATH / "disconnected.svg")

EYE  = str(BASE_PATH / "eye.svg")

EYE_OFF = str(BASE_PATH / "eye-off.svg")

CROSSHAIR = str(BASE_PATH / "crosshair.svg")

DOWNLOAD = str(BASE_PATH / "download.svg")

PEN_TOOL = str(BASE_PATH / "pen-tool.svg")

SCISSORS = str(BASE_PATH / "scissors.svg")

TRASH = str(BASE_PATH / "trash.svg")

MOVE = str(BASE_PATH / "move.svg")

THICKNESS = str(BASE_PATH / "thickness.svg")


