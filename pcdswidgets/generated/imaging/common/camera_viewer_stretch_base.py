"""
Generated by jinja from ui_base_widget.j2 with:
ui_name = camera_viewer_stretch.ui
form_cls = Ui_Form
base_cls = CameraViewerStretchBase
macro_names = ['NICKNAME', 'PREFIX', 'cam_prefix']

Other long required variables:
widget_names: list[str]
widget_name_to_class: dict[str, str]
macro_to_widget: dict[str, str]
widget_to_macro: dict[str, str]
widget_to_pre_templ_strs: dict[str, list[tuple[str, str]]]
widget_to_pre_templ_lists: dict[str, list[tuple[str, list[str]]]]
"""
# ruff: noqa: E501
# ruff: noqa: F403
# ruff: noqa: F405

from pcdswidgets.builder.designer_widget import DesignerWidget

from .camera_viewer_stretch_form import *

try:
    from qtpy.QtCore import pyqtProperty
except ImportError:
    from qtpy.QtCore import Property as pyqtProperty  # type: ignore


class CameraViewerStretchBase(DesignerWidget):
    PyDMLabel: "PyDMLabel"
    active_pv_label: "QLabel"
    nickname_label: "QLabel"

    ui_form = Ui_Form
    _macro_to_widget = {
        "NICKNAME": [
            "nickname_label",
        ],
        "PREFIX": [
            "active_pv_label",
        ],
        "cam_prefix": [
            "PyDMLabel",
        ],
    }
    _widget_to_macro = {
        "PyDMLabel": [
            "cam_prefix",
        ],
        "active_pv_label": [
            "PREFIX",
        ],
        "nickname_label": [
            "NICKNAME",
        ],
    }
    _widget_to_pre_template = {
        "PyDMLabel": [
            ("channel", "ca://${cam_prefix}:FRAMERATE_RBV"),
        ],
        "active_pv_label": [
            ("text", "PV: ${PREFIX}"),
        ],
        "nickname_label": [
            ("text", "${NICKNAME}"),
        ],
    }

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._macro_values = {
            "NICKNAME": "",
            "PREFIX": "",
            "cam_prefix": "",
        }

    def get_nickname(self) -> str:
        return self._get_macro("NICKNAME")

    def set_nickname(self, value: str) -> None:
        self._set_macro("NICKNAME", value)

    nickname = pyqtProperty(str, get_nickname, set_nickname)

    def get_prefix(self) -> str:
        return self._get_macro("PREFIX")

    def set_prefix(self, value: str) -> None:
        self._set_macro("PREFIX", value)

    prefix = pyqtProperty(str, get_prefix, set_prefix)

    def get_cam_prefix(self) -> str:
        return self._get_macro("cam_prefix")

    def set_cam_prefix(self, value: str) -> None:
        self._set_macro("cam_prefix", value)

    cam_prefix = pyqtProperty(str, get_cam_prefix, set_cam_prefix)
