name: Prototype Pixi Testing

# For testing: I'm looking for the best way to deploy our workflows
# Working theory: specifying jobs is better than having a big workflow with many inputs
# Result: we can easily separate workflows by permissions and keep most builds read-only

on:
  push:
    branches:
      - "master"
  pull_request:
  release:
    types:
      - created

jobs:
  pre-commit:
    permissions:
      contents: read
    uses: pcdshub/pcds-ci-helpers/.github/workflows/pre-commit.yml@abbea1b18bc622e2f0ba0c1b3672d057d5c87a4c

  unit-tests:
    permissions:
      contents: read
    uses: pcdshub/pcds-ci-helpers/.github/workflows/python-pixi-test.yml@abbea1b18bc622e2f0ba0c1b3672d057d5c87a4c

  packaging-tests:
    permissions:
      contents: read
    uses: pcdshub/pcds-ci-helpers/.github/workflows/python-uv-build.yml@abbea1b18bc622e2f0ba0c1b3672d057d5c87a4c
    with:
      package-name: "pcdswidgets"

  pypi-upload:
    permissions:
      id-token: write
    needs: packaging-tests
    if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags')
    uses: pcdshub/pcds-ci-helpers/.github/workflows/python-uv-publish.yml@abbea1b18bc622e2f0ba0c1b3672d057d5c87a4c

  docs-build:
    permissions:
      contents: read
    uses: pcdshub/pcds-ci-helpers/.github/workflows/python-pixi-docs-build.yml@abbea1b18bc622e2f0ba0c1b3672d057d5c87a4c

# TODO: not existing yet
#  docs-upload:
#    permissions:
#      pages: write
#      id-token: write
#    needs: docs-build
#    if: github.event_name == 'push' && startsWith(github.event.ref, 'refs/tags')
#    uses: pcdshub/pcds-ci-helpers/.github/workflows/python-docs-upload.yml@abbea1b18bc622e2f0ba0c1b3672d057d5c87a4c
